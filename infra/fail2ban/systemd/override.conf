# Sobe depois do Docker: as jails da DMZ protegem containers publicados, e o
# waf precisa ter criado o access log antes de o fail2ban começar a lê-lo.
[Unit]
After=docker.service
Wants=docker.service
